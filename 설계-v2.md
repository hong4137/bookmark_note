# 독서 노트 v2 — Cloudflare 이전 설계

> 2026-09-20 작성. v1(Apps Script) 설계는 [설계.md](설계.md).
> **데이터 모델·이어붙이기 판정 규칙은 v1 그대로 쓴다.** 바뀌는 것은 실행 환경과 화면이다.

---

## 1. 왜 옮기나

v1을 실제로 써 보고 나온 불만 네 가지다.

| 불만 | 원인 | v2에서 |
|---|---|---|
| 느리다 | 화면 이동마다 서버 왕복(0.5~2초) + 매번 시트 전체 읽기 | 첫 로딩에 전부 받아 브라우저가 들고 있음. 이동은 즉시 |
| 디자인이 별로다 | 기본 스타일에 가까움 | 종이·활자 쪽으로 전면 재설계 |
| 노트에 책 구분이 약하다 | 그냥 안 만듦 | 책별 묶음·필터 |
| 봇 답장이 1분 늦다 | Apps Script 폴링 | 웹훅을 Worker가 받아 즉시 |

Apps Script는 "빨리 돌아가는 것"을 만드는 데는 좋았다. 실제로 파싱 품질·데이터 모델·
이어붙이기 규칙이 맞는지는 v1으로 이미 검증했다. v2는 그 위에 실행 환경만 갈아 끼운다.

---

## 2. 구조

```
  [텔레그램]
      │ 웹훅 (즉시)
      ▼
  ┌──────────────────────────────────┐
  │  Cloudflare Worker               │
  │   · 텔레그램 웹훅 수신            │
  │   · Gemini 문장 파싱              │
  │   · 이어붙이기 판정               │
  │   · 앱 API                        │
  │   · 앱 화면(정적 파일) 서빙        │
  └───┬───────────────┬──────────────┘
      │               │
      ▼               ▼
  ┌────────┐   ┌──────────────────────┐
  │   D1   │   │  Apps Script 어댑터   │
  │ 페이지 │   │  사진 → 드라이브 저장  │  ← 남는 유일한 구글 조각
  │  노트  │   │  공개 링크 반환        │
  └────────┘   └──────────────────────┘
      ▲                    │
      │                    ▼
  [앱 브라우저] ───────▶ [드라이브 사진]
```

### 왜 Apps Script가 남나

사진을 구글 드라이브에 보관하기로 했는데, 드라이브에 쓰려면 구글 인증이 필요하다.
Worker에는 그 인증이 없고, 넣으려면 OAuth 클라이언트·동의 화면·도메인 심사를 다시
거쳐야 한다(명함봇에서 넘었던 그 벽이다).

그래서 **"사진을 받아 드라이브에 넣고 링크를 돌려주는" 일만 하는 어댑터**로 남긴다.
약 40줄이고, 파싱·판정·저장 로직은 전부 Worker로 간다.

### 사진이 링크로 공개되는 점

드라이브 파일을 `링크가 있는 모든 사용자` 로 공유해야 브라우저가 바로 띄울 수 있다.
주소를 아는 사람은 볼 수 있다는 뜻이다. 주소는 32자 무작위 ID라 추측은 어렵지만,
**완전한 비공개는 아니다.** 책 페이지 사진이라 감수하기로 한 선택이다.

나중에 신경 쓰이면 Cloudflare R2로 옮기면 된다. 그때는 어댑터도 같이 사라진다.

---

## 3. D1 스키마

시트 컬럼을 거의 그대로 옮긴다. 이름만 영문으로 바꾼다(SQL에서 다루기 쉽게).

```sql
CREATE TABLE pages (
  id         TEXT PRIMARY KEY,   -- p_20260920_0031
  book       TEXT NOT NULL,
  page       INTEGER,            -- 쪽번호. 못 읽으면 NULL
  shot_at    TEXT NOT NULL,      -- ISO8601
  sentences  TEXT NOT NULL,      -- JSON 배열
  starts_mid INTEGER NOT NULL,   -- 0/1
  ends_mid   INTEGER NOT NULL,
  prev_id    TEXT,               -- 이어붙인 앞 페이지
  photo_id   TEXT,               -- 드라이브 파일 id
  raw        TEXT                -- 문장을 합친 전문 (검색용)
);

CREATE TABLE notes (
  id        TEXT PRIMARY KEY,
  page_id   TEXT NOT NULL,
  book      TEXT NOT NULL,
  page      INTEGER,
  idx       INTEGER NOT NULL,    -- 페이지 안에서 몇 번째 문장인가
  text      TEXT NOT NULL,       -- 확정된 문장 (수정·병합 반영)
  memo      TEXT,
  saved_at  TEXT NOT NULL,
  UNIQUE(page_id, idx)
);

CREATE TABLE state (k TEXT PRIMARY KEY, v TEXT);        -- 현재 책 등
CREATE TABLE app_tokens (token TEXT PRIMARY KEY, created_at TEXT);
```

`notes.text` 가 `pages.sentences` 의 **사본**인 것은 v1과 같다.
원문을 고쳐도 확정한 노트는 흔들리지 않아야 한다.

---

## 4. 앱 인증

앱은 나만 열 수 있어야 하는데, `*.workers.dev` 는 Cloudflare Access를 걸 수 없다
(도메인이 있어야 한다). 그래서 **봇이 발급하는 토큰**으로 막는다.

```
텔레그램에서 /앱  →  봇이 일회성 링크를 답장
                     https://…workers.dev/?t=<32자 토큰>
                  →  앱이 토큰을 localStorage 에 저장
                  →  이후 모든 API 호출에 Authorization: Bearer <토큰>
```

토큰은 `app_tokens` 에 저장하고, 없는 토큰은 401. 유출이 의심되면 봇에서
`/앱초기화` 로 전부 지우고 새로 받으면 된다. 도메인을 사면 Cloudflare Access로 바꾼다.

**이 토큰이 이 시스템에서 가장 민감하다.** 링크만 있으면 밑줄을 전부 읽을 수 있다.
반대로 `TELEGRAM_SECRET`·`ADMIN_SECRET`·`DRIVE_SECRET` 은 쓰기·주입 권한이라
읽히는 피해는 없다.

시크릿이 비어 있을 때 검사가 통과해버리지 않도록, 두 창구 모두 **설정 여부를 먼저 본다**.
특히 `ADMIN_SECRET` 은 이전이 끝나면 지우는 것이 창구를 닫는 방법이므로,
지운 상태에서 빈 본문이 통과하면 그대로 구멍이 된다.

---

## 5. API

**핵심 원칙: 첫 로딩에 전부 받는다.** 느렸던 진짜 이유가 화면마다 왕복한 것이라서다.

| 메서드 | 경로 | 하는 일 |
|---|---|---|
| GET | `/api/bootstrap` | 책·페이지·문장·노트를 **한 번에** 전부 |
| POST | `/api/notes/toggle` | 문장 하나를 노트에 넣거나 뺀다 |
| POST | `/api/pages/:id/sentence` | 문장 수정 (노트도 같이 갱신) |
| POST | `/api/pages/:id/stitch` | 이어붙이기 끊기/다시잇기 |
| POST | `/tg/<secret>` | 텔레그램 웹훅 |

데이터가 작아서(문장 수천 개 = 수백 KB) 한 번에 받아도 부담이 없다.
받은 뒤 화면 이동은 전부 브라우저 안에서 일어나므로 **즉시**다.
쓰기는 화면을 먼저 바꾸고 서버에 보낸다(낙관적 갱신).

---

## 6. 화면 — 형광펜 책상

여섯 가지 방향을 실제로 만들어 비교한 뒤 고른 것이다.
처음 세 개(종이·활자 계열)는 전부 **배경이 아무 일도 하지 않아** 밋밋했다.
"발랄하게 가든 서재의 묵직함을 가져가든, 배경을 잘 줘라"가 그때 받은 지적이다.

| | 정한 것 | 이유 |
|---|---|---|
| 바탕 | 파스텔 색면(버터·민트·코럴)이 번진 위에 **모눈 노트 격자** | 배경이 분위기를 만들어야 한다 |
| 카드 | 흰 면 + **2px 검은 테두리 + 각진 그림자**, 살짝 기울임 | 책상 위에 툭 놓인 종이 느낌 |
| 강조 | 배경색이 아니라 **글자 아래 형광펜 자국** | "밑줄"이라는 이름과 동작이 맞아떨어진다 |
| 서체 | 전부 고딕(Pretendard). 손글씨체는 숫자·단위에만 | 한글은 화면에서 명조보다 고딕이 읽기 편하다 |
| 강조색 | 주황 하나 + 형광 노랑 + 민트 | 색을 늘리지 않는다 |
| 하단 탭 | 떠 있는 알약 | 화면을 가로지르는 바보다 가볍다 |

### 한글 서체를 고딕으로 간 근거

네이버 모바일 본문 연구에서 바탕(명조)보다 돋움(고딕)의 가독성이 우수하다고 나온다.
v2 첫 시안은 UI까지 전부 명조로 깔았는데 그게 "별로"의 큰 원인이었다.
명조는 **책에서 옮겨온 문장**에만 쓸 자리가 있고, 지금 방향에서는 아예 쓰지 않는다.

웹폰트는 `display=swap` 으로 깔아 **글자가 먼저 뜨고 나중에 바뀌게** 한다.
웹폰트를 기다리느라 빈 화면이 보이면 또 "느리다"가 된다.

### 어두운 모드

밝고 발랄한 방향이지만 새벽에 읽는 일이 많아 어두운 모드도 같은 성격으로 만든다.
테두리·각진 그림자·형광펜 자국을 그대로 두고 색만 어둡게 맞춘다.

---

## 7. 옮기는 순서

| 단계 | 내용 |
|---|---|
| 1 | D1 스키마 + Worker 골격 + 앱 API |
| 2 | 앱 화면 전면 재작성 (스텁으로 먼저 검증) |
| 3 | 드라이브 어댑터(Apps Script) |
| 4 | 텔레그램 웹훅 + Gemini 파싱을 Worker로 |
| 5 | 시트 → D1 데이터 이전 |
| 6 | v1 봇 트리거 제거 (같은 봇을 둘이 보면 메시지를 뺏는다) |

**5번 전까지 v1은 계속 돌려도 된다.** 다만 6번에서 반드시 v1 트리거를 꺼야 한다.
텔레그램은 봇 하나에 웹훅과 `getUpdates` 를 동시에 허용하지 않는다.

---

## 8. 그대로 가져가는 것

v1에서 검증된 것들이라 손대지 않는다.

- Gemini 출력 스키마(`page_number` / `sentences` / `starts_mid_sentence` / `ends_mid_sentence`)
  - v2에서 `kind` / `book_title` / `book_author` 를 더했다. 같은 호출로 표지인지 본문인지
    가려내 표지면 책을 정하고, 본문이면 문장을 쪼갠다. 사진 한 장에 호출도 한 번이다.
  - 알라딘이 표지를 못 주면 **방금 찍은 사진**을 표지로 쓴다. 어차피 드라이브에 있다.
- 프롬프트 (본문만, 각주·쪽번호 제외, 못 읽으면 `▯`)
- 이어붙이기 판정: 쪽번호가 둘 다 있으면 연속일 때만, 없으면 10분 이내
- 원본 데이터를 바꾸지 않고 `prev_id` 로 연결만 하는 방식
- 503 자동 재시도
