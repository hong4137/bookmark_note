# 밑줄 — 내가 할 일

> 이것만 보고 끝까지 갈 수 있게 썼습니다. 위에서부터 순서대로.
> 막히면 각 단계의 «막히면» 을 먼저 보세요.

**새로 만드는 건 Cloudflare Worker 와 D1 둘뿐입니다.**
텔레그램 봇, Gemini 키, Apps Script 프로젝트, 드라이브 폴더, 시트는 전부 쓰던 것 그대로.

---

## 값 적어두는 칸

단계를 오가며 옮겨 붙일 값들입니다. 나올 때마다 여기 적어두면 헤매지 않습니다.

| | 값 | 어디서 나오나 |
|---|---|---|
| 봇 토큰 | | 기존 Apps Script → ⚙️ 프로젝트 설정 → 스크립트 속성 → `TELEGRAM_TOKEN` |
| Gemini 키 | | 같은 곳 → `GEMINI_API_KEY` |
| `DRIVE_SECRET` | | 1단계 |
| 어댑터 `/exec` 주소 | | 1단계 |
| `database_id` | | 2단계 |
| 내 챗 ID | | 3단계 |
| `TELEGRAM_SECRET` | | 4단계에서 내가 아무 긴 문자열로 정함 |
| `ADMIN_SECRET` | | 4단계에서 내가 아무 긴 문자열로 정함 |
| Worker 주소 | | 4단계 |

---

## 1단계 — 기존 Apps Script 에 어댑터 얹기 (5분)

사진은 계속 구글 드라이브에 둡니다. Worker 에는 구글 인증이 없어서,
"사진을 받아 드라이브에 넣고 id 를 돌려주는" 조각만 Apps Script 에 남깁니다.

- [ ] <https://script.google.com> → **기존 `book-bot` 프로젝트** 열기 (새로 만들지 않음)
- [ ] **＋ → 스크립트** → 이름 `Adapter` → [book-notes/book-bot/Adapter.gs](book-notes/book-bot/Adapter.gs) 내용 붙여넣기
- [ ] [book-notes/book-bot/appsscript.json](book-notes/book-bot/appsscript.json) 을 저장소 것으로 **교체** ← 웹 앱 배포 설정이 들어갑니다
- [ ] 💾 저장
- [ ] **`v2_어댑터_암호만들기`** 실행 → 로그의 `DRIVE_SECRET` 을 위 표에 적기
- [ ] **배포 → 새 배포 → 웹 앱** / 실행: **나** / 액세스: **모든 사용자**
- [ ] `/exec` 주소를 위 표에 적기
- [ ] **`v2_어댑터_저장확인`** 실행 → `저장 성공` 이 뜨면 통과

> **왜 액세스가 「모든 사용자」인가** — Worker 가 로그인 없이 불러야 합니다.
> 대신 `DRIVE_SECRET` 을 모르면 아무것도 못 합니다.

«막히면» `PHOTO_FOLDER_ID 가 비어 있습니다` → 기존 `step2_저장소_생성` 을 안 돌린 것.
그것부터 실행하세요.

---

## 2단계 — D1 만들기 (3분)

```bash
cd "D:/원드라이브/OneDrive/개발/통합 개발/book-notes/worker"
npx wrangler login
npx wrangler d1 create booknote
```

- [ ] 출력된 `database_id` 를 위 표에 적기
- [ ] [book-notes/worker/wrangler.toml](book-notes/worker/wrangler.toml) 의 `database_id` 를 그 값으로 교체
- [ ] 스키마 올리기

```bash
npx wrangler d1 execute booknote --remote --file=schema.sql
```

---

## 3단계 — 내 챗 ID 알아내기 (2분)

`wrangler.toml` 의 `ALLOWED_CHATS` 에 넣을 값입니다. 여기 없는 사람은 봇을 못 씁니다.

- [ ] 기존 Apps Script 에서 **`상태확인`** 실행
- [ ] `ALLOWED_CHAT_IDS:` 줄의 숫자를 위 표에 적기

«그 값이 비어 있으면» 텔레그램에서 **@userinfobot** 에게 아무 말이나 보내면
`Id: 123456789` 를 알려줍니다.

---

## 4단계 — 설정 채우고 배포 (10분)

- [ ] [book-notes/worker/wrangler.toml](book-notes/worker/wrangler.toml) 의 `[vars]` 채우기

```toml
[vars]
ALLOWED_CHATS     = "내 챗 ID"
DRIVE_ADAPTER_URL = "1단계의 /exec 주소"
APP_URL           = ""      # 배포 후 채웁니다
```

- [ ] 시크릿 5개 넣기 (물어보면 값을 붙여넣고 Enter)

```bash
npx wrangler secret put TELEGRAM_BOT_TOKEN
npx wrangler secret put TELEGRAM_SECRET
npx wrangler secret put GEMINI_API_KEY
npx wrangler secret put DRIVE_SECRET
npx wrangler secret put ADMIN_SECRET
```

> `TELEGRAM_SECRET` 과 `ADMIN_SECRET` 은 내가 정하는 아무 긴 문자열입니다.
> 예: `booknote-hook-8f2a9c1d4b` / `booknote-admin-3e7d5a2f`. 표에 적어두세요.

- [ ] 배포

```bash
npx wrangler deploy
```

- [ ] 출력된 주소(`https://bookmark-note.○○○.workers.dev`)를 표에 적기
- [ ] `wrangler.toml` 의 `APP_URL` 에 그 주소를 넣고 **다시 `npx wrangler deploy`** 실행

---

## 5단계 — 봇을 Worker 로 돌리기 (3분) ⚠️

**순서를 지켜야 합니다.** 반대로 하면 두 봇이 메시지를 서로 뺏습니다.
텔레그램은 봇 하나에 웹훅과 폴링을 동시에 허용하지 않습니다.

- [ ] ① 기존 Apps Script 에서 **`트리거_제거`** 실행 ← **먼저**
- [ ] ② 웹훅 걸기

```bash
curl "https://api.telegram.org/bot<봇토큰>/setWebhook?url=<Worker주소>/tg&secret_token=<TELEGRAM_SECRET>"
```

- [ ] `{"ok":true}` 확인
- [ ] 봇에 **`/start`** → 답이 오면 성공

**봇 주소도 대화 내용도 그대로입니다.** 뒤에서 일하는 쪽만 바뀐 겁니다.

«답이 안 오면» 다른 창에서 `npx wrangler tail` 을 켜두고 다시 `/start` 를 보내세요.
실시간 로그에 원인이 찍힙니다.

---

## 6단계 — 쌓아둔 데이터 옮기기 (2분)

- [ ] 기존 Apps Script `Setup.gs` ([book-notes/book-bot/Setup.gs](book-notes/book-bot/Setup.gs)) 에서 `WORKER_URL` 과 `ADMIN_SECRET` 채우기
- [ ] **`v2_D1로_이전`** 실행
- [ ] 로그의 `HTTP 200` 과 `{"ok":true,...}` 확인

여러 번 실행해도 안전합니다(같은 id 는 덮어씁니다).

---

## 7단계 — 앱 열기 (2분)

- [ ] 봇에 **`/앱`** → 링크 받기
- [ ] 폰 브라우저에서 열기 → **홈 화면에 추가**
- [ ] 책 목록에 기존 기록이 보이면 이전 성공

---

## 8단계 — 진짜로 써보기

- [ ] 봇에 `/책 책이름`
- [ ] 책 페이지 사진 전송 → 문장 목록 답장 확인
- [ ] 앱에서 문장 탭 → 밑줄이 그어지는지
- [ ] 앱 **원본** 버튼 → 사진이 뜨는지 ← 어댑터가 제대로 붙었는지 확인하는 지점

여기까지 되면 끝입니다.

---

## 다 되면 정리할 것

- [ ] 드라이브 `독서노트/원본사진` 에서 `어댑터_설치확인.png` 삭제
- [ ] 기존 Apps Script `Setup.gs` 의 `SETUP`, `WORKER_URL`, `ADMIN_SECRET` 값을 `''` 로 비우기
      (값은 스크립트 속성에 이미 있어서 코드에 남아 있을 필요가 없습니다)

> 기존 Apps Script 프로젝트는 **지우지 마세요.** 트리거만 꺼진 상태로
> 드라이브 어댑터 역할을 계속합니다.

---

## 문제가 생기면

| 증상 | 볼 곳 |
|---|---|
| 봇이 무응답 | `npx wrangler tail` (실시간 로그) |
| "지정된 사용자만" | `wrangler.toml` 의 `ALLOWED_CHATS` |
| 앱이 "권한 없음" | 봇에 `/앱` 으로 새 링크 |
| 사진이 안 뜸 | 어댑터 `/exec` 를 브라우저로 열어보기. `DRIVE_SECRET` 양쪽 일치 확인 |
| Gemini 404 | 모델 은퇴. [book-notes/worker/src/index.js](book-notes/worker/src/index.js) 의 `GEMINI_MODEL` 교체 |
| 글자를 못 읽음 | 사진 다시. 펼친 양면으로, 평평하게 |

---

## 이건 별개입니다

**명함봇**은 이 작업과 무관하게 아직 남아 있습니다.
회사에서 친구와 함께 할 때 [namecard-bot/진행상황.md](namecard-bot/진행상황.md) 를 보세요.
