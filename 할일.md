# 밑줄 — 내가 할 일

> 이것만 보고 끝까지 갈 수 있게 썼습니다. 위에서부터 순서대로.
> 막히면 각 단계의 «막히면» 을 먼저 보세요.

**새로 만드는 건 Cloudflare Worker 와 D1 둘뿐입니다.**
텔레그램 봇, Gemini 키, Apps Script 프로젝트, 드라이브 폴더, 시트는 전부 쓰던 것 그대로.

---

## 값 적어두는 칸

단계를 오가며 옮겨 붙일 값 9개입니다. 1단계에서 나온 값을 4단계에서 쓰는 식이라,
**나올 때마다 메모장 같은 데 모아두세요.** 아래 표는 "무엇을 모아야 하는지" 목록입니다.
(이 문서를 뷰어로 보고 있다면 표에 직접 못 씁니다. 에디터로 열면 채워 넣을 수 있습니다)

| | 값 | 어디서 나오나 |
|---|---|---|
| 봇 토큰 | | 기존 Apps Script → ⚙️ 프로젝트 설정 → 스크립트 속성 → `TELEGRAM_TOKEN` |
| Gemini 키 | | 같은 곳 → `GEMINI_API_KEY` |
| `DRIVE_SECRET` | | 1단계 | c634422234064bee9e7ed0c582b43a4e
| 어댑터 `/exec` 주소 | | 1단계 | https://script.google.com/macros/s/AKfycbwAxI7pMSEKyaUl9Q79ZmYYKTpDssgSDbYs9exSPWrfkJF8m2WDRwoec_hIl9NesiIN/exec
| `database_id` | | 2단계 | ea2ee78e-eae9-4aae-bd60-1bbbc55920e0
| 내 챗 ID | | 3단계 |
| `TELEGRAM_SECRET` | | 4단계에서 내가 아무 긴 문자열로 정함 |
| `ADMIN_SECRET` | | 4단계에서 내가 아무 긴 문자열로 정함 |
| Worker 주소 | | 4단계 |

---

## 1단계 — 기존 Apps Script 에 어댑터 얹기 (5분)

사진은 계속 구글 드라이브에 둡니다. Worker 에는 구글 인증이 없어서,
"사진을 받아 드라이브에 넣고 id 를 돌려주는" 조각만 Apps Script 에 남깁니다.

- [ ] <https://script.google.com> → **기존 `book-bot` 프로젝트** 열기 (새로 만들지 않음)
- [ ] **＋ → 스크립트** → 이름 `Adapter` → [book-notes/book-bot/Adapter.gs](book-notes/book-bot/Adapter.gs) 내용 붙여넣기
- [ ] [book-notes/book-bot/appsscript.json](book-notes/book-bot/appsscript.json) 을 저장소 것으로 **교체** ← 웹 앱 배포 설정이 들어갑니다
- [ ] 💾 저장
- [ ] **`v2_어댑터_암호만들기`** 실행 → 로그의 `DRIVE_SECRET` 적어두기 (4단계에서 씁니다)
- [ ] **배포 → 새 배포 → 웹 앱** / 실행: **나** / 액세스: **모든 사용자**
- [ ] `/exec` 주소 적어두기 (4단계에서 씁니다)
- [ ] **`v2_어댑터_저장확인`** 실행 → `저장 성공` 이 뜨면 통과

> **왜 액세스가 「모든 사용자」인가** — Worker 가 로그인 없이 불러야 합니다.
> 대신 `DRIVE_SECRET` 을 모르면 아무것도 못 합니다.

«막히면» `PHOTO_FOLDER_ID 가 비어 있습니다` → 기존 `step2_저장소_생성` 을 안 돌린 것.
그것부터 실행하세요.

---

## 2단계 — D1 만들기 (3분)

```bash
cd "D:/원드라이브/OneDrive/개발/통합 개발/book-notes/worker"
npx wrangler login
npx wrangler d1 create booknote
```

- [ ] 출력된 `database_id` 적어두기 (바로 아래에서 씁니다)
- [ ] [book-notes/worker/wrangler.toml](book-notes/worker/wrangler.toml) 의 `database_id` 를 그 값으로 교체
- [ ] 스키마 올리기

```bash
npx wrangler d1 execute booknote --remote --file=schema.sql
```

---

## 3단계 — 내 챗 ID 알아내기 (2분)

`wrangler.toml` 의 `ALLOWED_CHATS` 에 넣을 값입니다. 여기 없는 사람은 봇을 못 씁니다.

- [ ] 기존 Apps Script 에서 **`상태확인`** 실행
- [ ] `ALLOWED_CHAT_IDS:` 줄의 숫자 적어두기 (4단계에서 씁니다)

«그 값이 비어 있으면» 텔레그램에서 **@userinfobot** 에게 아무 말이나 보내면
`Id: 123456789` 를 알려줍니다.

---

## 4단계 — 설정 채우고 배포 (10분)

- [ ] [book-notes/worker/wrangler.toml](book-notes/worker/wrangler.toml) 의 `[vars]` 채우기

```toml
[vars]
ALLOWED_CHATS     = "내 챗 ID"
DRIVE_ADAPTER_URL = "1단계의 /exec 주소"
APP_URL           = ""      # 배포 후 채웁니다
```

- [ ] 시크릿 5개 넣기 (물어보면 값을 붙여넣고 Enter)

```bash
npx wrangler secret put TELEGRAM_BOT_TOKEN
npx wrangler secret put TELEGRAM_SECRET
npx wrangler secret put GEMINI_API_KEY
npx wrangler secret put DRIVE_SECRET
npx wrangler secret put ADMIN_SECRET
```

> `TELEGRAM_SECRET` 과 `ADMIN_SECRET` 은 내가 정하는 아무 긴 문자열입니다.
> 예: `booknote-hook-8f2a9c1d4b` / `booknote-admin-3e7d5a2f`.
> `TELEGRAM_SECRET` 은 5단계에서, `ADMIN_SECRET` 은 6단계에서 다시 쓰니 적어두세요.
>
> **1회성이 아니라 계속 유효한 열쇠입니다.** 채팅·스크린샷에 흘리지 마세요.
> 아래 「보안」 절에 흘렸을 때 대처가 있습니다.

- [ ] 배포

```bash
npx wrangler deploy
```

- [ ] 출력된 주소(`https://bookmark-note.○○○.workers.dev`) 적어두기 (5·6단계에서 씁니다)
- [ ] `wrangler.toml` 의 `APP_URL` 에 그 주소를 넣고 **다시 `npx wrangler deploy`** 실행

---

## 5단계 — 봇을 Worker 로 돌리기 (3분) ⚠️

**순서를 지켜야 합니다.** 반대로 하면 두 봇이 메시지를 서로 뺏습니다.
텔레그램은 봇 하나에 웹훅과 폴링을 동시에 허용하지 않습니다.

- [ ] ① 기존 Apps Script 에서 **`트리거_제거`** 실행 ← **먼저**
- [ ] ② 웹훅 걸기 — **값 두 개를 실제 값으로 바꿔서** 붙여넣으세요

```powershell
$token  = "기존 봇 토큰"
$worker = "https://bookmark-note.○○○.workers.dev"
$secret = "4단계에서 정한 TELEGRAM_SECRET"

Invoke-RestMethod "https://api.telegram.org/bot$token/setWebhook?url=$worker/tg&secret_token=$secret"
```

> **PowerShell 에서 `curl "...<봇토큰>..."` 을 그대로 쓰면 안 됩니다.**
> `curl` 은 `Invoke-WebRequest` 별칭이고 `< >` 는 리디렉션 기호라 명령이 깨집니다
> (`>>` 에서 멈추면 **Ctrl+C**). 위 `Invoke-RestMethod` 형태를 쓰세요.

- [ ] `ok : True` · `description : Webhook was set` 확인
- [ ] 봇에 **`/start`** → 답이 오면 성공

확인:

```powershell
Invoke-RestMethod "https://api.telegram.org/bot$token/getWebhookInfo" | ConvertTo-Json
```

`url` 이 내 Worker 주소이고 `last_error_message` 가 없으면 정상입니다.

**봇 주소도 대화 내용도 그대로입니다.** 뒤에서 일하는 쪽만 바뀐 겁니다.

«답이 안 오면» 다른 창에서 `npx wrangler tail` 을 켜두고 다시 `/start` 를 보내세요.
실시간 로그에 원인이 찍힙니다.

---

## 6단계 — 쌓아둔 데이터 옮기기 (2분)

- [ ] 먼저 `Setup.gs` 를 [book-notes/book-bot/Setup.gs](book-notes/book-bot/Setup.gs) 로 **교체**
      (`Ctrl+A` → `Ctrl+V`. 이전 함수는 나중에 추가한 거라 옛 파일엔 없습니다)
- [ ] `WORKER_URL` 과 `ADMIN_SECRET` 채우고 💾 저장

```js
const WORKER_URL = 'https://bookmark-note.○○○.workers.dev';
const ADMIN_SECRET = '4단계에서 정한 값';
```

- [ ] **`v2_D1로_이전`** 실행
- [ ] 로그의 `HTTP 200` 과 `{"ok":true,...}` 확인

여러 번 실행해도 안전합니다(같은 id 는 덮어씁니다).

---

## 7단계 — 앱 열기 (2분)

- [ ] 봇에 **`/앱`** → 링크 받기
- [ ] 폰 브라우저에서 열기 → **홈 화면에 추가**
- [ ] 책 목록에 기존 기록이 보이면 이전 성공

---

## 8단계 — 진짜로 써보기

- [ ] 봇에 `/책 책이름`
- [ ] 책 페이지 사진 전송 → 문장 목록 답장 확인
- [ ] 앱에서 문장 탭 → 밑줄이 그어지는지
- [ ] 앱 **원본** 버튼 → 사진이 뜨는지 ← 어댑터가 제대로 붙었는지 확인하는 지점

여기까지 되면 끝입니다.

---

## 다 되면 정리할 것

- [ ] **`ADMIN_SECRET` 지우기** ← 이전이 끝나면 다시 쓸 일이 없습니다

```bash
npx wrangler secret delete ADMIN_SECRET
npx wrangler deploy
```

  지우면 `/admin/import` 창구가 영구히 닫힙니다(404). 나중에 또 옮길 일이 생기면
  그때 다시 넣으면 됩니다.

- [ ] 드라이브 `독서노트/원본사진` 에서 `어댑터_설치확인.png` 삭제
- [ ] 기존 Apps Script `Setup.gs` 의 `SETUP`, `WORKER_URL`, `ADMIN_SECRET` 값을 `''` 로 비우기
      (값은 스크립트 속성에 이미 있어서 코드에 남아 있을 필요가 없습니다)

> 기존 Apps Script 프로젝트는 **지우지 마세요.** 트리거만 꺼진 상태로
> 드라이브 어댑터 역할을 계속합니다.

---

## 보안 — 각 열쇠가 하는 일

전부 **1회성이 아니라 계속 유효**합니다. 흘리면 아래대로 바꾸면 됩니다.

| 열쇠 | 흘리면 | 대처 |
|---|---|---|
| **앱 링크**(`?t=...`) | 링크만 있으면 **내 밑줄을 전부 읽습니다.** 가장 민감 | 봇에 **`/앱초기화`** → 전부 무효. `/앱` 으로 새로 받기 |
| `TELEGRAM_SECRET` | 내 대화창에 가짜 메시지를 밀어 넣을 수 있음(스팸·엉뚱한 책 이름). 데이터를 읽어가진 못함 | `wrangler secret put TELEGRAM_SECRET` 으로 새 값 → `deploy` → `setWebhook` 다시 |
| `ADMIN_SECRET` | 내 기록을 덮어쓸 수 있음(읽기는 못 함) | 이전이 끝나면 **지우는 게 최선**. 위 「다 되면 정리할 것」 참고 |
| `DRIVE_SECRET` | 내 드라이브 사진 폴더에 파일을 올릴 수 있음(읽기·삭제는 못 함) | Apps Script 스크립트 속성에서 값 바꾸고 Worker 시크릿도 같이 교체 |
| 봇 토큰 · Gemini 키 | 봇을 통째로 조종 / 내 할당량 소진 | @BotFather 에서 `/revoke` · AI Studio 에서 키 삭제 후 재발급 |

> **원본 사진**은 드라이브 링크로 공개됩니다. 주소(32자 무작위)를 아는 사람은 볼 수 있습니다.
> 완전한 비공개는 아니고, 책 페이지라 감수한 선택입니다.

---

## 문제가 생기면

| 증상 | 볼 곳 |
|---|---|
| 봇이 무응답 | `npx wrangler tail` (실시간 로그) |
| "지정된 사용자만" | `wrangler.toml` 의 `ALLOWED_CHATS` |
| 앱이 "권한 없음" | 봇에 `/앱` 으로 새 링크 |
| 사진이 안 뜸 | 어댑터 `/exec` 를 브라우저로 열어보기. `DRIVE_SECRET` 양쪽 일치 확인 |
| Gemini 404 | 모델 은퇴. [book-notes/worker/src/index.js](book-notes/worker/src/index.js) 의 `GEMINI_MODEL` 교체 |
| 글자를 못 읽음 | 사진 다시. 펼친 양면으로, 평평하게 |

---

## 이건 별개입니다

**명함봇**은 이 작업과 무관하게 아직 남아 있습니다.
회사에서 친구와 함께 할 때 [namecard-bot/진행상황.md](namecard-bot/진행상황.md) 를 보세요.
