# 독서 노트 봇 — 설계

> 2026-09-19 작성. 착수 전 합의된 구조를 기록한 문서.
> 설치 순서는 [README.md](README.md). 9절에 완료 상태와 만들면서 바뀐 점을 적어 두었다.

---

## 1. 해결하려는 문제

책을 읽다 좋은 문장을 만나면 사진을 찍는다. 그런데 그 사진이 **갤러리에 쌓여 노이즈**가 된다.
찍은 건 많은데 정작 다시 읽지 않고, 정리하려면 품이 많이 든다.

핵심은 "사진을 없애는 것"이 아니라 **노이즈를 내가 매일 보는 곳에서 치우는 것**이다.

### 두 층으로 나눈다

| 층 | 무엇이 들어가나 | 성격 |
|---|---|---|
| **원문 창고** | 찍은 페이지의 전체 텍스트 + 원본 사진 | 지저분해도 된다. 평소엔 안 본다 |
| **독서 노트** | 내가 직접 고른 문장만 | 실제로 내가 보는 곳. 깨끗해야 한다 |

창고가 있어야 파싱 오류를 나중에 원본과 대조해 고칠 수 있다.
용량은 문제가 아니다(3절 참고). **사진을 버리지 않는 것이 이 설계의 전제다.**

---

## 2. 전체 구조

```
  [텔레그램]
      │  /책 데미안       ← 읽는 책을 한 번 지정
      │  📷 페이지 사진
      ▼
  ┌─────────────────────────────┐
  │  book-bot (Apps Script)     │   1분 트리거로 메시지를 가져옴
  │  · 사진 수신                 │
  │  · Gemini로 문장 단위 파싱    │
  │  · 앞 페이지와 이어붙이기 판정 │
  └───────┬──────────┬──────────┘
          │          │
          ▼          ▼
   [드라이브]     [구글 시트]
   원본 사진      페이지 / 노트
                      ▲
          ┌───────────┴──────────────┐
          │  book-app  (Apps Script) │   웹앱 · 접근: 나만
          │  · 책 → 페이지 → 문장     │
          │  · 탭해서 하이라이트       │
          │  · 독서 노트 모아보기      │
          └──────────────────────────┘
                 폰 브라우저 → 홈 화면에 추가
```

### 왜 프로젝트를 둘로 나누나

처음 이유는 접근 권한이었다. Apps Script 웹앱은 **배포 하나당 접근 권한이 하나**인데,
봇 웹훅은 텔레그램이 로그인 없이 호출해야 하므로 `모든 사용자`, 앱은 내 독서 기록이므로
`나만`이어야 했다. 한 프로젝트로는 둘을 동시에 만족할 수 없었다.

**폴링으로 바꾸면서 그 제약 자체는 사라졌다**(봇은 이제 배포를 쓰지 않는다).
그래도 나눠 둔다 — 봇은 1분마다 도는 백그라운드 작업이고 앱은 내가 여는 화면이라,
실행 기록·트리거·권한이 섞이지 않는 편이 고장 났을 때 원인을 찾기 쉽다.

두 프로젝트가 **같은 시트와 같은 드라이브 폴더**를 공유한다.

### 명함봇과의 차이

명함봇(`../namecard-bot`)과 뼈대는 같다. 다만 **전부 내 계정**이라
OAuth 클라이언트 · 동의 화면 · 도메인 · 심사가 전혀 필요 없다.
People API 대신 `ScriptApp.getOAuthToken()` 없이 SpreadsheetApp / DriveApp을 직접 쓴다.

준비물은 **새 텔레그램 봇 토큰**과 **Gemini API 키** 둘뿐이다.
(Gemini 키는 명함봇 것을 재사용해도 되지만, 한도를 따로 보려면 새로 발급하는 편이 낫다)

---

## 3. 용량과 한도

| 항목 | 수치 |
|---|---|
| 텔레그램 압축 사진 1장 | 약 200~400KB (최대 1280px) |
| 하루 10장 × 1년 | 약 0.9GB |
| 구글 무료 용량 | 15GB (Gmail·드라이브·포토 공용) |
| 15GB를 채우려면 | 약 6만 장 |
| Gemini 무료 등급 | 하루 수백 건 — 독서량으로는 닿지 않음 |
| Apps Script | UrlFetch 2만 건/일, 실행 6분/건 |

**결론: 용량은 제약이 아니다.** 원본 사진을 보관한다.

> 단, 15GB는 Gmail과 공용이다. 착수 전 <https://one.google.com/storage> 에서 잔여 용량 확인.

---

## 4. 데이터 구조

구글 시트 한 개에 탭 두 개.

### 시트 `페이지` — 원문 창고

| 컬럼 | 예시 | 설명 |
|---|---|---|
| `page_id` | `p_20260919_0031` | 고유 ID |
| `책` | `데미안` | 촬영 시점의 현재 책 |
| `쪽` | `87` | Gemini가 읽은 쪽번호. 없으면 공란 |
| `촬영일시` | `2026-09-19 21:04` | |
| `문장들` | `["...","..."]` | JSON 배열 |
| `앞에서_이어짐` | `TRUE` | 첫 문장이 앞 페이지의 연속인가 |
| `뒤로_이어짐` | `TRUE` | 마지막 문장이 끝나지 않았는가 |
| `이전_page_id` | `p_20260919_0030` | 이어붙인 상대. 없으면 공란 |
| `사진_id` | `1AbC...` | 드라이브 파일 ID |
| `원문` | `그는 오래 침묵했다. ...` | 문장들을 합친 전문. 검색용 |

### 시트 `노트` — 독서 노트

| 컬럼 | 예시 | 설명 |
|---|---|---|
| `note_id` | `n_20260919_0007` | |
| `page_id` | `p_20260919_0031` | 출처 페이지 |
| `책` | `데미안` | |
| `쪽` | `87` | |
| `문장` | `새는 알에서 나오려고 투쟁한다.` | **최종 텍스트** — 수정·병합이 반영된 값 |
| `문장_index` | `3` | 페이지 내 위치 (정렬용) |
| `메모` | | v2에서 사용 |
| `저장일시` | `2026-09-19 21:06` | |

> `노트.문장`은 `페이지.문장들`의 사본이다. 원문을 나중에 고쳐도 노트는 그대로 남는다.
> 노트가 "내가 확정한 것"이고 창고는 "찍힌 그대로"다. 둘을 분리해두는 편이 안전하다.

### 현재 읽는 책

`ScriptProperties`의 `현재_책`에 저장. 시트를 쓸 필요가 없다.

---

## 5. 텔레그램 동작

| 입력 | 결과 |
|---|---|
| `/책 데미안` | 현재 책을 `데미안`으로 설정. 이후 사진은 전부 이 책으로 |
| `/책` | 지금 무슨 책인지 답 |
| 📷 사진 | 파싱 → 저장 → **파싱 결과를 답장으로** 보여줌 |
| `/앱` | 앱 링크 |
| 그 외 텍스트 | 안내 메시지 |

여러 책을 교차로 읽는 경우는 드물다는 전제. 캡션으로 매번 책 이름을 다는 것보다 편하다.

### 사진을 받았을 때 답장 예시

```
📖 데미안 87쪽 · 문장 6개

1. 그는 오래 침묵했다.
2. 새는 알에서 나오려고 투쟁한다.
...
6. 그는 문득 자신이 오래전부터

⟩ 마지막 문장이 다음 페이지로 이어집니다
앱에서 열기
```

**그 자리에서 눈으로 확인하는 것이 1차 검증**이다. 이상하면 즉시 다시 찍는다.

---

## 6. Gemini 파싱

### 출력 스키마

```json
{
  "page_number": 87,
  "sentences": ["그는 오래 침묵했다.", "새는 알에서 나오려고 투쟁한다."],
  "starts_mid_sentence": false,
  "ends_mid_sentence": true
}
```

시트 컬럼은 한글, Gemini 응답 키는 영문이다. 코드에서 다루기 쉽고, 시트는 사람이 보는 것이라서.

`responseSchema`로 구조를 고정한다(명함봇과 같은 방식).

### 프롬프트 요점

- 본문만 추출. **쪽번호·각주 번호·머리말·챕터 제목은 문장에서 제외**
- 문장 단위로 쪼갠다. 문장이 끊긴 채 페이지가 끝나면 **그 조각을 그대로** 마지막 원소로
- 양면 펼침이면 **왼쪽 → 오른쪽** 순서
- 읽을 수 없는 글자는 지어내지 말고 `▯`로 표시
- 쪽번호를 못 찾으면 `null`

### 정확도에 대한 현실적 기대

인쇄된 한글 본문은 대체로 잘 읽지만 **완벽하지 않다.** 주로 어긋나는 곳:

- 책이 휘어진 부분 — 행이 겹치거나 늘어남
- 각주 번호가 본문에 섞임
- 조판 따옴표, 줄 끝 하이픈, 들여쓰기
- 그림자·손가락이 걸린 부분
- 한자, 전문용어, 외국어 인명

**대응은 세 겹이다.**

1. 찍은 직후 봇 답장으로 눈 확인
2. 앱에서 문장 옆 **[원본]** 으로 사진 대조
3. 앱에서 **문장 수정** — 고친 값이 노트에 저장됨

---

## 7. 페이지를 넘어가는 문장

### 판정

새 페이지를 저장할 때, 같은 책의 **직전 페이지**와 비교한다.

> 직전 `뒤로_이어짐` = TRUE **그리고** 새 페이지 `앞에서_이어짐` = TRUE
> → 이어붙임으로 표시

한글은 종결어미가 뚜렷해서(`~했다` vs `~하던`) 이 판정이 비교적 정확하다.

### 잘못 붙이지 않기 위한 추가 조건

책을 앞뒤로 오가며 찍으면 엉뚱한 페이지끼리 붙을 수 있다. 그래서:

- **쪽번호를 둘 다 읽었으면** — `87 → 88` 처럼 연속일 때만 (가장 확실)
- **쪽번호가 없으면** — 직전 사진과 10분 이내일 때만

이어붙였으면 **답장에 알린다.** 틀렸으면 그 자리에서 안다.

### 저장은 건드리지 않는다

이어붙이기는 **`이전_page_id` 로 연결만** 하고, 문장 자체는 각 페이지에 찍힌 그대로 남긴다.
합쳐 보여주는 것은 앱의 몫이다.

```
87쪽  ... 그는 문득 자신이 오래전부터   ⟩ 88쪽과 이어짐
88쪽  ⟨ 이어짐  그것을 알고 있었다는 사실을 깨달았다.
```

앱에서는 한 줄로 보이고, 하이라이트하면 **합쳐진 전체 문장**이 노트에 저장된다.
판정이 틀렸으면 **[끊기]** 로 분리한다. 원본이 그대로라 되돌리기가 안전하다.

> 양면 펼침으로 찍으면 한 사진 안에서 이어지므로 이 문제가 아예 생기지 않는다.
> 가능하면 펼침 촬영이 제일 깔끔하다.

---

## 8. 앱 화면

폰 브라우저로 열고 **홈 화면에 추가**하면 아이콘이 생겨 앱처럼 쓴다.

```
① 홈 — 책 목록
   데미안        42쪽 · 하이라이트 17
   토지 1권      88쪽 · 하이라이트 31

② 책 → 페이지 목록 (최신순)
   87쪽  그는 오래 침묵했다...        ● 미검토
   86쪽  ...                          

③ 페이지 — 문장 목록
   ┌────────────────────────┐
   │ [원본 사진 보기]  87쪽    │
   ├────────────────────────┤
   │  그는 오래 침묵했다.      │  ← 탭 = 하이라이트
   │  새는 알에서 나오려고     │  ← 하이라이트됨 (노란 밑줄)
   │  투쟁한다.               │
   │  ...                 ✎ │  ← ✎ 를 누르면 수정
   └────────────────────────┘

④ 독서 노트 — 하이라이트만
   전체 / 책별 필터
   "새는 알에서 나오려고 투쟁한다."  — 데미안 87쪽
```

**탭 하나 = 노트 저장.** 이 동작이 가벼워야 실제로 쓴다.

---

## 9. 만들 순서

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 시트·드라이브 폴더 생성, `book-bot` 골격 | ✅ |
| 2 | 사진 → Gemini 파싱 → 시트 저장 + 답장 | ✅ 실제 책으로 확인 |
| 3 | `book-app` — 책 → 페이지 → 문장, 하이라이트 토글 | ✅ |
| 4 | 독서 노트 화면 | ✅ |
| 5 | 문장 수정 · 원본 사진 보기 | ✅ |
| 6 | 페이지 넘김 이어붙이기 | ✅ 판정·끊기·다시잇기 |

### 만들면서 바뀐 것

- **웹훅 → 폴링.** Apps Script 웹앱은 `/exec` 에 항상 302 를 돌려주는데 텔레그램이 그걸
  실패로 보고 재시도하며, 그동안 뒤 메시지가 큐에 막혀 영영 전달되지 않는다.
  1분 트리거로 `getUpdates` 를 부르는 방식으로 바꿨다. 답장이 최대 1분 늦는 대신 확실하다.
- **사진을 파싱보다 먼저 저장.** Gemini 과부하(503)가 실제로 자주 나서, 원본을 먼저 남기고
  실패하면 다음 폴링 때 자동으로 다시 시도한다(최대 6회). 사람이 다시 보낼 일이 없다.
- **모델 은퇴에 대비.** `gemini-2.5-flash` 가 설치 중에 은퇴했다. `모델_확인` 함수로
  쓸 수 있는 모델을 조회해 `GEMINI_MODEL` 만 바꾸면 되도록 해뒀다.

---

## 10. v1에 넣지 않는 것

일부러 뺀 것들이다. 쓰다 보면 어디가 아쉬운지 드러나므로 그때 붙인다.

- 검색
- 문장별 메모 / 태그
- 노트 내보내기 (구글 문서, 마크다운)
- 복습 알림
- 여러 책 동시 진행
- 하이라이트에 색상 구분

---

## 11. 결정 기록

| 결정 | 이유 |
|---|---|
| 책 이름은 `/책` 명령으로 한 번만 | 여러 책을 교차로 읽는 일이 드물다. 매 사진 캡션은 번거롭다 |
| 페이지 전체를 파싱 | 찍을 때 고르는 것(키워드 입력)이 더 번거롭다. 고르는 일은 앱에서 |
| 원본 사진 보관 | 용량이 제약이 아니고, 파싱 오류를 복구할 유일한 수단 |
| 문장 배열로 저장 | 앱에서 문장 하나가 탭 단위가 되어야 하이라이트가 가벼워진다 |
| 노트는 문장의 **사본** | 원문을 고쳐도 확정한 노트가 흔들리지 않는다 |
| 프로젝트 2개 분리 | 웹앱 접근 권한이 배포당 하나뿐 (웹훅=공개, 앱=비공개) |
| 이어붙이기는 표시만 | 원본 데이터를 바꾸지 않아야 오판을 되돌릴 수 있다 |
